{
  "name": "talk-hub",
  "version": "0.0.5",
  "description": "",
  "repository": "github:StudioProcess/talk-hub",
  "scripts": {
    "start": "sudo live-server --port=80 --browser='google chrome' --ignore=.git,node_modules .",
    "old": "sudo live-server old --mount='/node_modules:node_modules' --port=80 --browser='google chrome'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "scrape": "node --experimental-modules scrape.mjs"
  },
  "author": "Process Studio <contact@process.studio> (https://process.studio)",
  "license": "AGPL-3.0",
  "dependencies": {
    "gsap": "^2.1.3",
    "jquery": "^3.5.1",
    "jquery-color": "^2.2.0",
    "live-server": "^1.2.1",
    "seedrandom": "^3.0.5"
  },
  "devDependencies": {
    "cheerio": "^1.0.0-rc.3",
    "request": "^2.88.2",
    "request-promise-native": "^1.0.9"
  }
}
